pipeline { 
    agent any
    environment {
        DOCKERHUB_CREDENTIALS = credentials('Docker_ID')
    }
     stages {
        stage('Build Docker Image') {
            steps { 
                sh 'docker build -t newimage/alpine:latest .'
                }
            }
        stage('login to dockerhub'){
            steps {
            sh 'echo $DOCKERHUB_CREDENTIALS_PSW | sudo docker login -u $DOCKERHUB_CREDENTIALS_USR'
            }
        }

        stage('push image'){
            steps {
                sh 'docker push newimage/alpine:latest '
            }
        }
    }
         post { 
            success { 
                echo "Build is successfull" 
            } 
            failure { 
                echo "Build failed" 
            }
        }
    }
